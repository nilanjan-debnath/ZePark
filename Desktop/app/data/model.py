# import tensorflow as tf


def get():
    # model_path = "app/data/resource/model/ZePark_bimodel v1.256.6.tflite"
    # interpreter = tf.lite.Interpreter(model_path=model_path)
    # interpreter.allocate_tensors()
    # return interpreter
    return None
